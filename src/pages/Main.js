import styled from 'styled-components';

const Main = () => {
    return (
        <MainWrap>
            메인페이지
        </MainWrap>
    );
};

const MainWrap = styled.div`
    margin-left: 200px;
`
export default Main;