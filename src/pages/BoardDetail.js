import styled from 'styled-components';

const BoardDetail = () => {
    return (
        <BoardDetailWrap>
            보드페이지
        </BoardDetailWrap>
    );
};

const BoardDetailWrap = styled.div`
    margin-left: 200px;
`
export default BoardDetail;